pipeline {
    agent any

    stages {
        stage('run parallel') {
            parallel {
                stage('windows') {
                    agent {
                        label "windows"
                    }
                    steps {
                        System.out.println(System.properties['os.name'])
                    }
                }
                stage('linux') {
                    agent {
                        label "unix"
                    }                
                    steps {
                        System.out.println(System.properties['os.name'])
                    }
                }
            }
        }
    }
}
