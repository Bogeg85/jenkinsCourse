pipeline {
    agent any

    stages {
        stage('run parallel') {
            parallel {
                stage('windows') {
                    agent {
                        label "windows"
                    }
                    steps {
                        echo System.properties['os.name']
                    }
                }
                stage('linux') {
                    agent {
                        label "unix"
                    }                
                    steps {
                        echo System.properties['os.name']
                    }
                }
            }
        }
    }
}
