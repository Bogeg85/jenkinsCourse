pipeline {
    agent any

    stages {
        stage('run parallel') {
            parallel {
                stage('windows') {
                    agent {
                        label "SlaveWindows"
                    }
                    steps {
                        bat "winver"
                    }
                }
                stage('linux') {
                    agent {
                        label "unix"
                    }                
                    steps {
                        sh "uname -r"
                    }
                }
            }
        }
    }
}
